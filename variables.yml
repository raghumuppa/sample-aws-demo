# Sandbox variables — randomized PER allocated sandbox, so every trainee gets
# unique answers. Referenced by provisioning/playbook.yml (to configure the box)
# and by training.json (as the expected answers).
telnet_port:
  type: port
  min: 1500
root_flag:
  type: text
